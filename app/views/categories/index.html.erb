
<h1>Category List</h1>
<%= link_to "Add New Category", new_category_path , class: "btn py-2 ml-2 bg-blue-600 text-white" %>
<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %> 
      <%= link_to "Edit", edit_category_path(category) %>
      <%= button_to "🗑", category_path(category), method: :delete %>
    </li>
  <% end %>
</ul>
<%= link_to "Back To Task", tasks_path, class: "btn py-2 ml-2 bg-blue-600 text-white" %>


